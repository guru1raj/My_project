Your Final Command List (A1 Complete)
git config --global user.name "Name"
git config --global user.email "Email"

git init
git status

touch file1.txt
echo "text" >> file1.txt

git add .
git commit -m "message"

git log

git remote add origin <repo link>
git push -u origin main

git clone <repo link>

git branch
git branch -M main

git reset --soft HEAD~1
